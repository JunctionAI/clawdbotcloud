{
  "name": "stripe-setup-automation",
  "version": "1.0.0",
  "description": "One-command setup for Stripe products and payment links",
  "main": "stripe-setup-automation.js",
  "scripts": {
    "setup": "node stripe-setup-automation.js",
    "install-cli": "npm install -g stripe",
    "test": "echo 'Run: npm run setup' && exit 0"
  },
  "keywords": [
    "stripe",
    "automation",
    "payment-links"
  ],
  "author": "Tom",
  "license": "MIT",
  "dependencies": {
    "stripe": "^14.0.0",
    "supermemory": "^4.0.0"
  }
}
