# Task 009: Analytics

## Parallel Group: 1 (Foundation)
**Can run parallel with:** 001, 002, 003, 008
**Depends on:** Nothing
**Blocks:** Nothing

## Objective
Implement analytics to understand user behavior, measure activation, and inform product decisions.

## Requirements

### Must Have
- [ ] Page view tracking
- [ ] User identification (after auth)
- [ ] Key event tracking
- [ ] Conversion funnel (signup → activate → pay)
- [ ] Basic dashboard access

### Should Have
- [ ] Session recording/replay
- [ ] Feature usage tracking
- [ ] Error tracking integration
- [ ] A/B test framework
- [ ] Cohort analysis

### Nice to Have
- [ ] Custom dashboards
- [ ] Automated reports
- [ ] Anomaly detection
- [ ] Revenue analytics (MRR, churn)

## Analytics Stack

### Recommended: PostHog
- Open source option available
- Self-hostable
- Product analytics + session replay
- Feature flags built-in
- Good free tier

### Alternatives:
- Mixpanel (powerful, expensive)
- Amplitude (enterprise)
- Plausible (privacy-focused, simpler)

## Key Events to Track

### Acquisition
- `page_view` (all pages)
- `landing_cta_click`
- `signup_started`
- `signup_completed`

### Activation
- `onboarding_started`
- `onboarding_step_completed` (step number)
- `onboarding_completed`
- `first_message_sent`
- `first_response_received`

### Engagement
- `message_sent`
- `conversation_created`
- `skill_used` (skill name)
- `settings_changed`

### Revenue
- `checkout_started`
- `subscription_created` (plan)
- `subscription_upgraded`
- `subscription_canceled`

### Retention
- `session_started`
- `return_visit` (days since last)

## Technical Implementation

### PostHog Setup
```typescript
// lib/analytics.ts
import posthog from 'posthog-js';

export const analytics = {
  init: () => {
    posthog.init(process.env.NEXT_PUBLIC_POSTHOG_KEY!, {
      api_host: 'https://app.posthog.com',
      capture_pageviews: true,
    });
  },
  
  identify: (userId: string, traits?: Record<string, any>) => {
    posthog.identify(userId, traits);
  },
  
  track: (event: string, properties?: Record<string, any>) => {
    posthog.capture(event, properties);
  },
  
  page: (name?: string) => {
    posthog.capture('$pageview', { page: name });
  },
};
```

### Event Properties Standard
```typescript
// Every event should include:
{
  // Auto-captured
  timestamp: ISO8601,
  session_id: string,
  user_id?: string,
  
  // Context
  page_url: string,
  referrer?: string,
  
  // Event-specific
  ...eventProperties
}
```

## Key Metrics Dashboard

### North Star
- **Weekly Active Conversations** (engagement)
- **Messages per User per Day** (depth)

### Funnel
```
Visitors → Signups → Activated → Subscribers → Retained
   100%      10%        40%          5%          80%
```

### Health Metrics
- DAU/MAU ratio (stickiness)
- Average messages per session
- 7-day retention
- Time to first value

## Acceptance Criteria
1. All key events firing correctly
2. Users identified after login
3. Funnel visible in dashboard
4. Session replay working
5. No PII in analytics (email hashed)
6. GDPR consent flow if EU users

## Privacy Considerations
- [ ] Cookie consent banner
- [ ] Respect Do Not Track
- [ ] Option to opt-out
- [ ] No sensitive data in events
- [ ] Data retention policy

## Estimated Effort
- Provider setup: 0.5 days
- Event implementation: 0.5 days
- Dashboard setup: 0.5 days
- Testing/validation: 0.5 days
- **Total: 1-2 days**

## Notes
- Start simple, add events as needed
- Don't over-instrument initially
- Focus on conversion funnel first
- Review weekly to identify gaps
